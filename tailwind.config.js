/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./*.html",         // all HTML files in root
    "./**/*.html"       // all HTML files in subfolders
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}